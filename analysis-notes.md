# تحليل الحالة الحالية والتعديلات المطلوبة

## المشكلة الحالية
PerformanceEvidence.tsx (3100+ سطر) يحتوي على:
- نماذج مكررة لكل شاهد (كل شاهد له نموذج كامل)
- AI مكرر داخل كل حقل إدخال
- لا يوجد عرض مزدوج (جدولي + تفاعلي) داخل الصفحة
- لا يوجد معاينة/تصدير لكل تقرير على حدة

## المكونات الموجودة بالفعل (جاهزة)
1. ReportForm.tsx (592 سطر) - نموذج إدخال ديناميكي مع AI مركزي
2. ReportPreview.tsx (474 سطر) - معاينة حية بعرض جدولي وتفاعلي
3. ReportEditor.tsx (369 سطر) - واجهة موحدة تجمع الإدخال والمعاينة
4. TemplateManager.tsx (515 سطر) - لوحة إدارة القوالب (PDF themes فقط)

## الجداول الموجودة
- reportTemplates - قوالب التقارير (fields + layout)
- userReports - تقارير المستخدمين (data مملوءة)
- pdfTemplates - ثيمات PDF
- portfolios - ملفات الإنجاز

## API الموجودة
- reportTemplates.list/listAll/get/create/update/delete/seed
- userReport.list/get/create/update/delete
- ai.fillFormFields/improveText/suggest/classifyEvidence/suggestEvidence/analyzeGaps

## التعديلات المطلوبة

### 1. إعادة هيكلة PerformanceEvidence.tsx
- بدلاً من النماذج المكررة لكل شاهد، نموذج إدخال واحد
- المستخدم يختار الوظيفة -> يرى المعايير -> يضغط على معيار -> يفتح نموذج واحد
- داخل النموذج يضيف صفوف/شواهد ديناميكياً
- كل شاهد يحتوي على العناصر المطلوبة
- لكل تقرير خيارات معاينة وتصدير

### 2. مساعد AI مركزي واحد
- بدلاً من AI في كل حقل
- مساعد واحد يساعد في تعبئة البيانات وتحسين النصوص

### 3. عرض مزدوج
- عرض جدولي منظم
- عرض ديناميكي تفاعلي داخل القالب

### 4. لوحة إدارة القوالب (Admin)
- إضافة قوالب جديدة
- تعديل القوالب
- إضافة/حذف الحقول
- التحكم في طريقة عرض عناصر التقرير
- قوالب تفاعلية قابلة للتعديل من الإدارة

### 5. التصميم بهوية التعليم
- مطابق للنماذج المرفقة (header وزارة التعليم)
- إمكانية تغيير التصميم أو اختيار قوالب أخرى
