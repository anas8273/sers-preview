# تشخيص المشاكل

## مشكلة 1: الأيقونات المتراكبة
الصفحة تعرض البنود في قسمين متكررين:
1. شبكة بطاقات صغيرة (grid 4 أعمدة) - سطور 809-860
2. قائمة تفصيلية (space-y-2) - سطور 862-907

الحل: إزالة شبكة البطاقات الصغيرة (القسم الأول) والاكتفاء بالقائمة التفصيلية مع تحسين تصميمها لتكون أوضح وأجمل.

## مشكلة 2: اختفاء الصفحة عند التصنيف الذكي
classifyEvidence هو protectedProcedure (سطر 233 في routers.ts)
عند عدم تسجيل الدخول → يرجع خطأ UNAUTHORIZED → main.tsx يعيد التوجيه لصفحة تسجيل الدخول

الحل: تحويل classifyEvidence إلى publicProcedure لأنه لا يحتاج بيانات المستخدم فعلياً.
أو: إضافة try/catch في handleSmartUpload لمنع إعادة التوجيه.
